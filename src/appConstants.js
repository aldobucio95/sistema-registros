/** Constantes compartidas entre App y pantallas extraídas (evita import circular con App.jsx). */
/** v2: solo Campa y General (sin Bautizos ni Desayuno Conferencia). */
export const EVENT_TYPES = ['Campa', 'General'];
export const SI_LABEL = 'Sí';
export const PAYMENT_METHODS = ['Efectivo', 'Tarjeta'];
export const SERVICE_OPTIONS = ['Primero', 'Segundo', 'Tercero'];
