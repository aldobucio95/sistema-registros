/** v2 stub: waitlist registry expand helpers removed with Bautizos event type. */
export function expandBautizosWaitlistRegistryRows(rows) {
  return Array.isArray(rows) ? rows : [];
}

export function expandBautizosWaitlistRegistryDisplayRows(rows) {
  return Array.isArray(rows) ? rows : [];
}
